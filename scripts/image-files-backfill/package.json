{
  "name": "image-files-backfill",
  "private": true,
  "type": "module",
  "description": "One-off (DQX-49): measure seeded primaries, encode AVIF files, fix lying extensions/content-types across the R2 archive. Deletable once run. Not a workspace member — `npm install` here.",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "sharp": "^0.34.0"
  }
}
