{
  "name": "avif-backfill",
  "private": true,
  "type": "module",
  "description": "One-off: AVIF variants + extension re-key for existing R2 images. Deletable once run. Not a workspace member — `npm install` here.",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "sharp": "^0.34.0"
  }
}
